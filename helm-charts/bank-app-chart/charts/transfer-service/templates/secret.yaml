apiVersion: v1
kind: Secret
metadata:
  name: {{ include "transfer-service.fullname" . }}-secrets
  labels:
    {{- include "transfer-service.labels" . | nindent 4 }}
type: Opaque
data:
  database-password: {{ .Values.database.password | b64enc | quote }}